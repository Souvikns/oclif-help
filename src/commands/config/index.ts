import {Command} from '@oclif/core'

export default class ConfigCommand extends Command {
    static description: string | undefined = "update config of cli"
    async run(): Promise<any> {
        
    }
}